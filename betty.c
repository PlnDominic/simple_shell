#include <stdio.h>
#include <unistd.h>

int main(void)
{
    int x = 5; /* x is set to 5 */
    int y = 3; /* y is set to 3 */
    int diff = x - y; /* Calculate the difference between x and y */

    printf("diff = %d\n", diff); /* Print the difference */

    return (0);
}
